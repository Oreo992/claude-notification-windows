{
  "description": "Windows 桌面通知",
  "hooks": {
    "PermissionRequest": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/notify.ps1\" -Title \"Claude Code\" -Message \"需要权限确认\" -Dir \"${CLAUDE_PROJECT_DIR}\""
          }
        ]
      }
    ],
    "Notification": [
      {
        "matcher": "idle_prompt",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/notify.ps1\" -Title \"Claude Code\" -Message \"等待你的输入\" -Dir \"${CLAUDE_PROJECT_DIR}\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "powershell -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/notify.ps1\" -Title \"Claude Code\" -Message \"任务已完成\" -Dir \"${CLAUDE_PROJECT_DIR}\""
          }
        ]
      }
    ]
  }
}
